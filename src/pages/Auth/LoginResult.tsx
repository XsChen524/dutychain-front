import { PageContainer } from "@ant-design/pro-components";

const LoginResultPage: React.FunctionComponent<{}> = (props) => {
	return (
		<PageContainer
			ghost
			title={'Result'}
		>
			Result
		</PageContainer>
	);
};

export default LoginResultPage;
